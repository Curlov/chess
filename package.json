{
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:chess": "vite build --base=/chess/",
    "deploy:chess": "vite build --base=/chess/ && rsync -av --delete dist/ root@deathstar:/srv/www/chess/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}

